# VS Code Notes for Blender Development

This folder can contain VS Code workspace settings, tasks, and notes specific to Blender development.

Recommended resource:

https://www.youtube.com/watch?v=_0srGXAzBZE

Example next steps:
- Add `settings.json` to configure Python interpreter or formatting
- Add `tasks.json` to run Blender with `--python` for quick testing